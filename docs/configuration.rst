Configuration
=============

Pipeline options are controlled via a YAML config file. Pass yours with ``--config /path/to/config.yaml`` (see :doc:`usage_notes` :ref:`command-line-reference`).

Configuration generator
-----------------------

**Recommended.** Use the interactive generator to choose options and download a ready-to-use YAML file:

- `Configuration generator <_static/config_generator.html>`_
