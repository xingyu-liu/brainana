Other documentation
===================

Process and pipeline details are in :doc:`processing` (built with the docs).

Design and development docs (in the repository, not part of the Read the Docs build):

- **docs/paper/** — Design and architecture, core components.
- **docs/RESOURCE_CALIBRATION.md** — Resource tuning (CPUs, memory, Nextflow).
- **docs/PARAMETER_MANAGEMENT.md** — Parameter and config handling.
- **README_Docker.md** — Docker usage summary (mirrored in :doc:`usage_local` and :doc:`faq`).

For user-facing usage, start with :doc:`installation` and :doc:`usage_local`.
